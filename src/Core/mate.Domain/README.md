// mate.Domain — the only project referenced by all other projects.
// It contains no framework dependency — pure C# class library.
// All contracts (interfaces) are defined here so modules never need to reference Core.
